import { connect } from 'react-redux';

import MyIndexComponent from 'components/my/MyIndexComponent';

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(MyIndexComponent)
