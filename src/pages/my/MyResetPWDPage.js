import { connect } from 'react-redux';

import MyResetPWDComponent from 'components/my/MyResetPWDComponent';

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(MyResetPWDComponent)
